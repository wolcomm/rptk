---
language: python
python:
  - "2.7"
sudo: required
before_install:
  - ./install-bgpq3.sh
install:
  - pip install -r packaging/requirements-test.txt
  - pip install -e .
script:
  - nosetests --with-coverage
after_success:
  - codecov
...